server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: bookapp

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

jwt:
  secret: ${JWT_SECRET:local-dev-jwt-secret-key-12345678901234567890}

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:admin1234}